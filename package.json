{
  "name": "us-presidents-quiz",
  "version": "0.1.0",
  "private": true,
  "description": "A static multiple-choice quiz about US presidents.",
  "scripts": {
    "test": "node --test \"tests/**/*.test.mjs\"",
    "validate": "node tools/validate-questions.mjs"
  },
  "engines": {
    "node": ">=22"
  }
}
